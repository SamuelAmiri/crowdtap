<div style='width: 100%;'>
  <div id="geolocation" style="width: 100%; height: 500px;"></div>
  <%= render "/layouts/home_map"  %>
</div>

<center><h3>--Recently Added Beers--</h3></center>
<% @news_feed.each do |f| %>
    <div class="col-md-4 col-sm-6">
      <div class="news_box">
        <div class="button_div first_button">
          <h4>
            <center><%= @beer.find(f.beer_id).name%> <br><br>has been added to <br><br><%= @restaurant.find(f.restaurant_id).name%><br><br><%= @restaurant.find(f.restaurant_id).address%>
            </center>
          </h4>
              
        </div>
      </div>
    </div>
  <% end %>



