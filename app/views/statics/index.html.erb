<div style='width: 100%;'>
  <div id="mapwrapper"><div id="geolocation" style="width: 100%; height: 500px;"></div>
  <%= render "/layouts/home_map"  %></div>
</div>

<center><h3>--Recently Added Beers--</h3></center>
<% @news_feed.each do |f| %>
    <div class="col-md-4 col-sm-6">
      <div class="news_box">
        <div class="button_div first_button">
          <h4>
            <center> <a href="/beers/<%= f.beer_id%>"><%=@beer.find(f.beer_id).name%></a> <br><br>has been added to <br><br><a href="/restaurants/<%= f.restaurant_id%>"><%=@restaurant.find(f.restaurant_id).name%></a><br><br><%= @restaurant.find(f.restaurant_id).address%>
            </center>
          </h4>
              
        </div>
      </div>
    </div>
  <% end %>



